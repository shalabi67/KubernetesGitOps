apiVersion: v1
kind: Service
metadata:
  name: {{ include "mon.fullname" . }}
  labels:
    {{- include "mon.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mon.selectorLabels" . | nindent 4 }}
